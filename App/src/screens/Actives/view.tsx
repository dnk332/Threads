import React from 'react';
import AppContainer from '@components/AppContainer';
import AppText from '@components/AppText';

const ActivesScreenView = () => {
  return (
    <AppContainer>
      <AppText>ActivesScreenView</AppText>
    </AppContainer>
  );
};

export default ActivesScreenView;
